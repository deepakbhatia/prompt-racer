# Challenge: hello-http

Public spec: `packages/challenges/src/hello-http.ts`

Private evaluator assets go under `golden/` (gitignored + agent-ignored):

- `golden/tests/` — automated checks
- `golden/reference/` — optional reference implementation for maintainers
