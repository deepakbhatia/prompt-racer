# Challenge catalog (docs + private golden trees)

Public challenge **specs** live in `packages/challenges` (imported by the web app and agents).

This folder holds:

- Per-challenge notes / authoring docs
- `golden/` trees for automated checks (gitignored + agent-ignored except `.gitkeep`)

## hello-http

See `packages/challenges/src/hello-http.ts` for the public brief.
Private evaluator assets: `hello-http/golden/` (local only).

## todo-cli

See `packages/challenges/src/todo-cli.ts` for the public brief.
Private evaluator assets: `todo-cli/golden/` (local only).

## notes-api

See `packages/challenges/src/notes-api.ts` for the public brief.
Private evaluator assets: `notes-api/golden/` (local only).

## reading-list

See `packages/challenges/src/reading-list.ts` for the public brief.
Private evaluator assets: `reading-list/golden/` (local only).
